{
  "name": "chopinly",
  "private": true,
  "type": "module",
  "description": "Piano practice assistant PWA \u2014 no build step, no dependencies",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "deploy": "npx wrangler pages deploy . --project-name woodshed --branch main",
    "dev": "npx wrangler pages dev . --local --port 8788",
    "db:migrate": "npx wrangler d1 migrations apply chopinly --remote",
    "db:migrate:local": "npx wrangler d1 migrations apply chopinly --local",
    "help": "node dev/build-help.mjs",
    "shots:help": "node dev/shoot-help.mjs"
  }
}

{
  "name": "chopinly",
  "private": true,
  "type": "module",
  "description": "Piano practice assistant PWA — no build step, no dependencies",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\""
  }
}

// The release the running app was built from. Shown in small print at the
// bottom of the account sheet so a phone can tell a new build has landed.
// Bump it together with CACHE in sw.js — tests/version.test.mjs insists.
export const VERSION = "v46";
